# PluginStatusBarAction

**Qualified name:** `mv::gui::PluginStatusBarAction`

`PluginStatusBarAction` associates one factory-owned status-bar item with a loaded plugin kind and can condition its visibility on that factory having active instances.

Begin with {doc}`Status-bar integration <../../../../../development/building_plugins/status_bar/index>` for the registration pattern, multi-instance semantics, and current runtime limitations.

```{doxygenclass} mv::gui::PluginStatusBarAction
:members:
:protected-members:
```
