# Parallel

`Parallel` provides blocking convenience operations for a single scheduled callable, parallel collection processing, ordered parallel mapping, and staged chain construction.

```{doxygenclass} mv::Parallel
:members:
```
