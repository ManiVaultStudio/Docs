# Workflow result registry

A process-wide registry for retaining and resolving results used by notification links and UI presentation.

```{doxygenclass} mv::workflow::WorkflowResultRegistry
:members:
```

