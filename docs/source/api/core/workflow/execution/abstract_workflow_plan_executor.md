# Abstract workflow plan executor

The public execution contract for root and nested plans, with blocking and asynchronous entry points.

```{doxygenclass} mv::workflow::AbstractWorkflowPlanExecutor
:members:
```

