# Workflow execution context

The node-local runtime interface to shared state, progress, reporting, outputs, and cancellation.

```{doxygenclass} mv::workflow::WorkflowExecutionContext
:members:
```

