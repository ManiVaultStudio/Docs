# ForegroundTask

**Qualified name:** `mv::ForegroundTask`

Use `ForegroundTask` for user-initiated work whose live state should appear automatically in the foreground task popup.

For selection, lifecycle, and cancellation guidance, see {doc}`Tasks and progress reporting <../../../../development/building_plugins/tasks/index>`.

```{doxygenclass} mv::ForegroundTask
:members:
:protected-members:
```
