# ModalTask

**Qualified name:** `mv::ModalTask`

Use `ModalTask` only when continuing to interact with the application while the operation runs would be invalid. Its handler presents a modal task dialog.

For selection, lifecycle, and cancellation guidance, see {doc}`Tasks and progress reporting <../../../../development/building_plugins/tasks/index>`.

```{doxygenclass} mv::ModalTask
:members:
:protected-members:
```
