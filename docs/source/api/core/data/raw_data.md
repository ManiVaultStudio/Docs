# Raw data plugin

`plugin::RawData` is the plugin base for a registered data kind. It creates concrete dataset implementations and reports the storage associated with its raw-data instance.

```{doxygenclass} mv::plugin::RawData
:members:
```
