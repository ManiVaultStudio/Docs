# Data type

`DataType` is the logical type identifier associated with a raw-data plugin and its dataset implementations. It supports comparison and hashing for filtering and registration.

```{doxygenclass} mv::DataType
:members:
```
