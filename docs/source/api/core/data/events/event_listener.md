# Event listener

`EventListener` registers with the core event manager and invokes callbacks for all dataset events or events filtered by data type and event type. Destruction unregisters the listener.

```{doxygenclass} mv::EventListener
:members:
```
