# Datasets

Datasets are stored in the {doc}`Data Manager <api/core/managers/abstract_data_manager>` and their lifetime is tracked by the model classes below.

```{toctree}
:maxdepth: 1

abstract_datasets_model
datasets_list_model
datasets_filter_model
```